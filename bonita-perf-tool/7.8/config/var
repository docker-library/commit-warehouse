# conf/conf.properties
${PERF_MODE}
${PERF_BROKER_ADDRESS}
${PERF_THINK_TIME}
# conf/conf.engine.properties
${PERF_REFPATH}

# conf/bonitaBPM6-tests-unitrunner.cfg.xml
# conf/bonitaBPM6-tests-loadrunner.cfg.xml
# conf/bonitaBPM6-tests-loadrunnerburstpick.cfg.xml
${PERF_TESTS}

# home_save/client/conf/bonita-client.properties
${PERF_BONITAURL}
# nb launch
${PERF_NB_LAUNCH}
# nb parallel launch
${PERF_NB_PARALLEL_LAUNCH}
# server mode (false = client, controls a separated Bonita instance || true = server, embedded server)
${PERF_SERVER_MODE}
# timeout process instance
${PERF_TIMEOUT_INSTANCE}
# timeout process task
${PERF_TIMEOUT_ACTIVITY}
# clean platform
${PERF_CLEAN_PLATFORM}
# clean tenantUser
${PERF_CLEAN_TENANT_USER}
# clean processInstance
${PERF_CLEAN_PROCESS_INSTANCE}
